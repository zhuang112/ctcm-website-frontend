# 轉向 Directus + Astro：遷移策略與落地步驟（ctworld）

> 目標：把目前「內容地基（AnyContent/JSON pipeline + crawler/QA）」保留下來，將 CMS 改為 **Directus**，前端改為 **Astro**，並維持既有多語策略與圖文規則。

---

## 核心原則（先別推倒重來）

1. **AnyContent/JSON 仍是 canonical（單一真相）**
   - 你的 crawler/HTML→JSON、QA、zh-TW→zh-CN（OpenCC）等「資料生產鏈」是最貴的資產。
   - 轉 CMS 不等於要重做抓取與清洗。

2. **Directus 只負責：資料結構、權限、管理介面、API**
   - Directus 會依據資料庫 schema 自動生成 REST/GraphQL API。citeturn0search4
   - Collections ≈ DB tables，改 collections 會改 DB schema。citeturn0search0

3. **Astro 負責呈現**
   - Astro 預設 build-time prerender；需要時可選擇 route on-demand rendering（SSR）。citeturn0search1
   - 可用 CMS/SDK 方式取內容。citeturn0search19turn0search23

---

## 三段式落地路線（最小風險）

### Phase 0：決策點（1 次定案，避免反覆）
**你需要做的選擇只有 3 個：**

A. **資料模型策略**
- A1「一套 AnyContent」：Directus 做一個 `any_content` 集合（含 type + payload JSON）→ 速度最快、彈性最大。
- A2「分集合（teaching/news/magazine/branch/…）」：查詢直覺、欄位型別化，但 migration & schema 維護較重。
- A3「混合」：核心用 `any_content`，而 SEO/路由常用欄位（title/slug/date/tags/cover）抽到頂層欄位。

B. **多語策略**
- B1 Directus Translations（官方 built-in translations 介面）。citeturn0search2turn0search10
- B2 每語系一筆 item（不建議：維運成本高、關聯困難）
- B3 只存 zh-TW，zh-CN runtime 用 OpenCC（最省，但管理介面不可見「實際中文(簡)」）

C. **Astro Rendering 模式**
- C1 全站 SSG（最簡單、最穩）
- C2 混合：大多 SSG + 少數 SSR（搜尋、個人化、表單等）。citeturn0search1turn0search17
- C3 全 SSR（通常沒必要）

# 決策:
這是一個非常紮實且考慮周全的遷移策略草案。ChatGPT 提出的方案在架構上是正確的（保留資料生產鏈、Headless CMS、現代前端）。

然而，**「10 萬筆資料（100k items）」的規模是最大的變數**。這個量級會直接讓某些「理論上可行」的選項在「實際上」變成災難（例如 Build time 過久、資料庫查詢過慢）。

以下我針對這三個核心決策點（A/B/C），結合 **10 萬筆資料規模** 與 **ctworld (佛教/內容型網站)** 的特性，為您做深入分析與推薦：

**決策點 A：資料模型策略 (Data Modeling)**

**現狀挑戰**：10 萬筆資料若全部塞在一個 JSON 欄位（A1），Postgres 雖可處理 JSONB 查詢，但 Directus 後台列表載入會變慢，且 API 篩選（Filtering）效能會下降。

- **選項分析**：
    - **A1 (One Big Collection)**：開發最快，但管理災難。10 萬筆資料混在一起，編輯要找一篇「2015 年的新聞」會很痛苦。
    - **A2 (分集合)**：最正規，但遷移成本高。需要為 News, Teaching, Sutras 各自建立 Collection。優點是資料庫索引（Index）效率最高。
    - **A3 (混合模式 - 推薦)**：這是最務實的折衷。
- **🏆 推薦選擇：A3 (混合模式) + 虛擬分類**
    - **作法**：建立一個核心 Table any_content。
    - **關鍵調整**：將 **「高頻查詢欄位」**（Title, Slug, PublishDate, Type, Category, Tags）**拉出來做成獨立的實體欄位（Columns）**並加上 Index。
    - **內容保留**：剩下的複雜結構（Body, Blocks, Metadata）保留在 payload (JSON) 欄位中。
    - **理由**：這樣做 API 查詢速度極快（因為是用 SQL Column 查），同時保留了 AnyContent 的彈性。

**決策點 B：多語策略 (Multilingual)**

**現狀挑戰**：佛教專有名詞多，OpenCC 自動轉換可能有誤（例如：「乾闥婆」轉簡體可能有歧義），且 10 萬筆資料若全部存兩份（B2），資料量變 20 萬筆，關聯維護困難。

- **選項分析**：
    - **B1 (Directus Translations)**：官方標準做法。資料庫會有一張 any_content_translations 表。
    - **B3 (Runtime OpenCC)**：前端即時轉。優點是 DB 輕量，缺點是**無法手動修正**簡體錯字。
- **🏆 推薦選擇：B1 (Directus Translations)**
    - **理由**：
        1. **修正能力**：作為權威內容網站（ctworld），您必須保留「手動修正簡體翻譯」的權利。B3 做不到這點。
        2. **搜尋優化**：繁簡體分開儲存，可以讓 Directus 的搜尋功能（Search Index）更準確地索引簡體關鍵字。
        3. **遷移腳本**：在匯入階段（Phase 3），您的 Script 可以呼叫 OpenCC 跑一次，將結果填入 zh-CN 的 translation 欄位。這樣既有自動化的便利，又有手動介入的空間。

**決策點 C：Astro Rendering 模式 (最關鍵的效能瓶頸)**

**現狀挑戰**：**這是最危險的決策點。** 10 萬頁若採用全靜態生成（C1 SSG），依照 Astro 的速度，Build 一次可能需要 30~60 分鐘甚至更久。只要改一個錯字就要重新 Build 全站，這在運維上是不可接受的。

- **選項分析**：
    - **C1 (全 SSG)**：**絕對不可行**。10 萬頁 Build time 會卡死 CI/CD。
    - **C3 (全 SSR)**：伺服器壓力大，且佛教網站多為靜態閱讀，沒必要每次都動態算。
- **🏆 推薦選擇：C2 (Hybrid 混合模式) + ISR 概念**
    - **具體架構**：
        1. **首頁、最新文章（前 100 篇）、核心單元**：使用 **SSG**（預先生成），保證速度最快。
        2. **歷史資料（剩下的 99,900 篇）**：設定為 **SSR (Server Side Rendering)** 但搭配 **HTTP Cache-Control (CDN)**。
        3. **或使用 Astro 的 "Server Islands" / ISR**：當使用者第一次訪問冷門舊文章時，Server 動態生成並快取起來（Cached on CDN/Edge），下一個人看就是靜態的。
    - **理由**：這樣 Build time 只需幾秒鐘（只 Build 核心頁面），而舊資料依然能被搜尋引擎抓取且讀取快速。

**綜合決策矩陣 (Decision Matrix)**

針對 ctworld.org 改版，建議鎖定以下組合：

| **決策點** | **建議選項** | **關鍵理由 (針對 10 萬筆資料)** |
| --- | --- | --- |
| **資料模型** | **A3 (混合)** | 兼顧 SQL 查詢效能 (Columns) 與 內容彈性 (JSON)。**務必對 slug, date, type 建 Index。** |
| **多語策略** | **B1 (官方翻譯表)** | 允許「人工校對」簡體內容，這對宗教專有名詞至關重要。 |
| **渲染模式** | **C2 (Hybrid)** | **避免全站 Build。** 僅預渲染「首頁/最新/熱門」，其餘採 SSR + CDN 快取 (SWR)。 |



> 建議預設：A3 + B1 + C2（穩定、可擴充、對內容站友好）

---

### Phase 1：Directus 先站起來（不動前端）
**Deliverables**
1. `docker-compose.yml`（directus + postgres + storage）
2. Directus 專案初始化：roles、public permissions、CORS、assets
3. 資料模型（collections + fields + relations）
4. 匯入腳本：把現有 JSON（AnyContent）灌進 Directus items

**模型建議（對應你既有 AnyContent）**
- `any_content`（核心）
  - `id` (uuid)
  - `type` (enum: teaching/news/magazine/branch/gallery/index_page…)
  - `lang` (enum: zh-TW/zh-CN/en/ja) 或用 translations
  - `slug` (string, unique per lang+type)
  - `title` (string)
  - `published_at` (datetime)
  - `meta` (json: seo, og, layout, etc.)
  - `body` (json 或 markdown/string 依你既有規則)
  - `blocks` (json：你既有結構化內容/圖文段落)
  - `cover_asset` (m2o to directus_files)
  - `images`（o2m：每張圖一筆，含 caption/alt/順序/來源）

> Directus 的 Items API/Singleton 用法可用 REST 或 GraphQL。citeturn0search8turn0search4

**多語（B1）**
- 對需要翻譯的集合新增 `translations` 欄位（type: Translations），會自動建立 `languages` + `*_translations`。citeturn0search10turn0search2
- 注意：要給 Public role 對 `languages` 與 `*_translations` 的 Read 權限（否則前端取不到）。citeturn0search10

---

### Phase 2：Astro 新前端（先做到「能讀 Directus、能出頁」）
**Deliverables**
1. Astro 專案骨架 + Directus SDK/REST fetch layer
2. 路由：`/[lang]/[type]/[slug]`
3. 靜態頁生成（SSG）+ 少數 SSR（必要才開）
4. 基礎 SEO（title/description/og）
5. 圖片輸出策略（Directus assets + width/quality 參數）

Directus 官方有「Directus + Astro」教學，可直接參考取數方式與動態頁生成思路。citeturn0search3

---

### Phase 3：把既有 pipeline 接到 Directus（最重要）
你現在的 pipeline 大致是：
HTML/crawler → AnyContent JSON →（zh-CN OpenCC）→ WP importer → Next 前端

換成：
HTML/crawler → AnyContent JSON →（zh-CN OpenCC 或 Directus translations）→ **Directus importer** → **Astro 前端**

**Importer 形態（推薦）**
- Node script：讀 `data/**/*.json` → 呼叫 Directus REST 批次 upsert
- 以 `external_id` 或（type+lang+slug）做 idempotent key
- 先上傳 files（或保留 remote URL，後續再搬）

---

## 你可以「今天就開始做」的最小步驟（不需要先把全部推翻）

1. 先做 **Phase 0 決策**（A/B/C 三個點）
2. 產出一個「最小資料模型」：any_content + files + images
3. 寫 importer：灌 20 筆內容進 Directus
4. Astro 做出 1 條動態路由讀這 20 筆，確認 SEO/圖片可用
5. 再擴充到 teaching/news/magazine 全量

---

## 需要你提供的最小輸入（比整包 repo 更有效）
- 目前 AnyContent schema（V1）規格與實際 JSON 範例（各 type 各 2 筆）
- 你現在前端路由規則（Next 版本的 URL 形式）
- 圖片/字幕規則（你目前已定：caption 存 JSON 不進 Markdown）
- 多語策略你要選 B1/B2/B3

---

## 下一個可落地的 INSTR（給 Codex）
**任務名建議：T-0092 Directus+Astro 基礎骨架（Phase 1+2 最小閉環）**
- 建 `apps/directus/`（compose）
- 建 `apps/astro/`（取數 + 一條 route）
- 建 `tools/directus-import/`（讀 AnyContent JSON → upsert items）
- 補 `docs/ARCHITECTURE_DIRECTUS_ASTRO.md`（包含 A/B/C 決策、資料模型、匯入流程、權限）

