---
import { fetchAnyContent } from "../../../lib/directus";
const { lang, type } = Astro.params;
const sample = await fetchAnyContent({ lang, type });
---

<html lang={lang}>
  <head>
    <title>{type} index</title>
  </head>
  <body>
    <h1>{type} index ({lang})</h1>
    <p>Placeholder listing page. Sample title: {sample.title}</p>
  </body>
</html>
